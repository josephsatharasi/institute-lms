/**
 * Student role — keep all student-only routes, pages, components, and hooks here.
 * Export public pieces from this file when you add them.
 */
export * from './pages';
export * from './components';
